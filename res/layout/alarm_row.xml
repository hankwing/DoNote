<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:background="#00000000"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:descendantFocusability="blocksDescendants" >

    <ImageButton
        android:id="@+id/alarm_set_alarm"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_alignBottom="@+id/alarm_checkBox"
        android:background="#00000000"
        android:focusable="false"
        android:src="@drawable/ic_alarm_set" />

    <TextView
        android:id="@+id/alarm_timeandcata"
        android:layout_width="wrap_content"
        android:layout_height="25dp"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:layout_marginLeft="49dp"
        android:gravity="center"
        android:textColor="#000000"
        android:singleLine="true"
        android:text="The First Alarm"
        android:textSize="18sp" />

    <CheckBox
        android:id="@+id/alarm_checkBox"
        style="@style/MyCheckBox"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:background="#00000000"
        android:focusable="true" />

    <TextView
        android:id="@+id/alarm_content"
        android:layout_width="wrap_content"
        android:layout_height="25dp"
        android:layout_alignLeft="@+id/alarm_timeandcata"
        android:gravity="center"
        android:textColor="#778899"
        android:singleLine="true"
        android:layout_alignBottom="@+id/alarm_checkBox"
        android:text="Eat meal"
        android:textSize="18sp" />

</RelativeLayout>